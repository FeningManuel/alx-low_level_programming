#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char m;

	m = 'u';
	while
		(m >= 'b') {
			putchar(m);
			m--;
		}
	putchar('\n');
	return (0);
}
