#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char m;

	m = 'z';
	while
		(m >= 'a') {
			putchar(m);
			m--;
		}
	putchar('\n');
	return (0);
}
