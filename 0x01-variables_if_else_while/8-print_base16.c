#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	char m;

	int d;

	m = 'a';
	d = 0;
	while
		(d < 10) {
			putchar(d + '0');
			d++;
		}
	while
		(m <= 'f') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
