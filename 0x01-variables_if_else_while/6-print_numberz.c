#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always 0 (Success)
 */

int main(void)
{
	int p;

	p = 0;
	while
		(p < 10) {
			putchar(p + '0');
			p++;
		}
	putchar('\n');
	return (0);
}
